# topogram-demo-todo

Generated by `topogram new` from the `@attebury/topogram-template-todo` template.

## Workflow

```bash
npm install
npm run explain
npm run check
npm run generate
npm run verify
```

Edit `topogram/` and `topogram.project.json`, then regenerate with `npm run generate`.
Generated app code is written to `app/`.
